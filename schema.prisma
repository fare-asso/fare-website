generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("SUPABASE_POSTGRES_PRISMA_URL")
  directUrl = env("SUPABASE_POSTGRES_PRISMA_DIRECT_URL")
}

model User {
  id              String       @id @default(uuid())
  name            String?
  image           String?
  createdAt       DateTime     @default(now())
  role            Role         @default(MEMBER)
  writtenArticles Article[]
  association     Association?
  createdEvents   Event[]
}

model Member {
  id           Int     @id @default(autoincrement())
  firstName    String
  lastName     String
  email        String
  facebookUrl  String?
  instagramUrl String?
  twitterUrl   String?
  picturePath  String
  position     String  @default("membre actif")
}

model Association {
  id               Int      @id @default(autoincrement())
  name             String
  major            String
  desc             String
  location         String
  email            String   @default("")
  discord          String?
  facebook         String?
  instagram        String?
  logoPath         String[]
  twitter          String?
  website          String?
  birthdate        DateTime @default(now())
  representativeId String?  @unique
  representative   User?    @relation(fields: [representativeId], references: [id])
}

model Event {
  id         Int      @id @default(autoincrement())
  name       String
  image      String
  desc       String
  createdAt  DateTime @default(now())
  startTime  DateTime
  endTime    DateTime
  location   String
  categoryId Int
  creatorId  String
  visibility Boolean  @default(false)
  category   Category @relation(fields: [categoryId], references: [id])
  createdBy  User     @relation(fields: [creatorId], references: [id])
}

model Category {
  id     Int     @id @default(autoincrement())
  name   String  @unique
  events Event[]
}

model Article {
  id         Int      @id @default(autoincrement())
  title      String
  authorId   String
  writtenOn  DateTime @default(now())
  content    Json
  imagesPath String[]
  author     User     @relation(fields: [authorId], references: [id])
}

model CommuniqueDePresse {
  id        Int      @id @default(autoincrement())
  name      String
  filePath  String
  createdAt DateTime @default(now())
  size      Int      @default(0)
}

model BagadAssoEquipment {
  id        Int               @id @default(autoincrement())
  name      String
  imagePath String?
  deposit   Float             @default(0)
  number    Int               @default(1)
  tickets   BagadAssoTicket[] @relation("BagadAssoEquipmentToBagadAssoTicket")
}

model BagadAssoTicket {
  id                    Int                  @id @default(autoincrement())
  assocation            String
  mail                  String
  firstName             String
  lastName              String
  phoneNumber           String?
  eventName             String
  eventType             String
  eventDate             DateTime
  eventAddr             String
  estimatedParticipants Int                  @default(0)
  creationDate          DateTime             @default(now())
  materials             BagadAssoEquipment[] @relation("BagadAssoEquipmentToBagadAssoTicket")
}

model LocationCache {
  id             Int      @id @default(autoincrement())
  query          String   @unique
  response       Json[]
  lastAccessedAt DateTime @default(now())
}

enum Role {
  MEMBER
  ADMIN
  ASSO_OWNER
}
