export function pageTitle(title?: string): string {
    return title ? `${title} | FARE` : "FARE"
}

export function dashboardTitle(title?: string): string {
    return title ? `${title} | Dashboard FARE` : "Dashboard"
}
